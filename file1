#include <iostream>
#include <string>

int getLength(const std::string& str) {
    return str.length();
}

int main() {
    std::string word = "Hello, World!";
    std::cout << getLength(word) << std::endl;
    return 0;
}

